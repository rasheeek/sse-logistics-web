export class Trip {
  id: string;
  addedBy: string;
  unitNumber: string;
  date: string;
  origin: string;
  destination: string;
  trailerNumber: string;
  manifestNumber: string;
  miles: string;
  remarks: string;
  trailerImage: string;
  manifestImage: string;
  teamMate: string;
}
